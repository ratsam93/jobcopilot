services:
  jobcopilot:
    container_name: jobcopilot
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: "8000"
    ports:
      - "8000:8000"
    restart: unless-stopped
